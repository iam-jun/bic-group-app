import * as React from 'react';
import {render} from '@testing-library/react-native';
import renderer from 'react-test-renderer';
import DraftPost from '~/screens/Post/DraftPost/index';

describe('DraftPost Screen', () => {
  it(`2 + 2 should be 4`, () => {
    expect(2 + 2).toBe(4);
  });
});
